rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Students collection - only admin can access
    match /students/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
    }
    
    // Volunteers collection - only admin can access
    match /volunteers/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
    }
    
    // Teachers collection - only admin can access
    match /teachers/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
    }
    
    // Notices collection - only admin can write, public can read
    match /notices/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Faculty collection - only authenticated users can write, public can read
    match /faculty/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Visiting faculty collection
    match /visiting_faculty/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Non-teaching staff collection
    match /non_teaching_staff/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Volunteer teachers collection
    match /volunteer_teachers/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Outlines collection
    match /outlines/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Notes collection
    match /notes/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Past papers collection
    match /past_papers/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Tools collection
    match /tools/{docId} {
      allow write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
      allow read: if true; // Public read access for display
    }
    
    // Backups collection - only admin can access
    match /backups/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
    }
    
    // Alerts collection - only admin can access
    match /alerts/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == 'admin@gece.com';
    }
  }
}
