import React from 'react';
import Admission from '../pages/admission';

const AdmissionForm = ({ application }) => (
    <Admission initialApplication={application} />
);

export default AdmissionForm;
