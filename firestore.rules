rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all read/write access for testing
    // This will fix the permissions error immediately
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
